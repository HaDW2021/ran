# 파이썬 random 함수
import random
# print(random.random())            # 0.0 <= x < 1.0 사이
# print(random.randint(1,10))         # 1에서 10 사이의 정수 중에서 난수값 리턴
# print(ramdom.uniform(10,20))        # min max 사이 float 리턴
# print(ramdom.randarange(10))        # 지정 범위 int 리턴
# print(ramdom.choice([1,2,3,4,5]))   # 리스트 내부에 있는 요소를 랜덤하게 선택
li = [1,2,3,4,5]                    
# print(random.sample(li,5))          # 리스트 요소를 중복 안 되게 뽑는다.
random.shuffle(li)                   # 리스트 요소를 다시 섞어서 리턴
print(li)
